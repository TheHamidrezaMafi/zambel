export type SortItem = {
  label: string;
  value: string;
};

export type SortRadioProperties = {
  item: SortItem;
};

export type SortModalProperties = {
  isLoading: boolean;
};

export type SortChipProperties = {
  isDisabled: boolean;
};
