# 🚀 راهنمای سریع راه‌اندازی پروژه با Docker

## نصب و راه‌اندازی

### مرحله 1: توقف کانتینرهای قبلی (در صورت وجود)

```bash
cd /Users/hamid/Desktop/Work/zambeel/zambel

# توقف کانتینرهای قبلی
docker stop zambeel-frontend-container zambeel-backend-container zambeel-scraper-container 2>/dev/null || true

# حذف کانتینرهای قبلی
docker rm zambeel-frontend-container zambeel-backend-container zambeel-scraper-container 2>/dev/null || true
```

### مرحله 2: راه‌اندازی با Docker Compose

#### روش ساده (پیشنهادی):

```bash
# راه‌اندازی همه سرویس‌ها با یک دستور
./docker.sh up
```

#### یا استفاده مستقیم از docker-compose:

```bash
docker-compose up -d --build
```

### مرحله 3: بررسی وضعیت

```bash
# نمایش وضعیت کانتینرها
./docker.sh status

# یا
docker-compose ps
```

### مرحله 4: مشاهده لاگ‌ها (اختیاری)

```bash
# مشاهده لاگ همه سرویس‌ها
./docker.sh logs-f

# یا برای هر سرویس جداگانه:
./docker.sh logs-frontend
./docker.sh logs-backend
./docker.sh logs-scraper
```

## 🌐 دسترسی به سرویس‌ها

پس از راه‌اندازی موفق، می‌توانید به آدرس‌های زیر دسترسی داشته باشید:

- **فرانت‌اند**: http://localhost:3000
- **بک‌اند API**: http://localhost:8080
- **داکیومنت API (Swagger)**: http://localhost:8080/api
- **اسکریپر**: http://localhost:5000

## 🔄 دستورات مفید

```bash
# توقف همه سرویس‌ها
./docker.sh down

# ری‌استارت همه سرویس‌ها
./docker.sh restart

# ساخت مجدد ایمیج‌ها (بعد از تغییر کد)
./docker.sh build
./docker.sh up

# پاکسازی کامل
./docker.sh clean
```

## 📊 معماری شبکه

```
zambeel-network (Bridge Network)
    ├── zambeel-scraper-container (5000:5000)
    ├── zambeel-backend-container (8080:8080)
    └── zambeel-frontend-container (3000:3000)
```

تمام سرویس‌ها در شبکه `zambeel-network` قرار دارند و می‌توانند با یکدیگر ارتباط برقرار کنند.

## 🔧 عیب‌یابی

### پورت در حال استفاده است:

```bash
# پیدا کردن پروسس روی پورت
lsof -i :3000
lsof -i :8080
lsof -i :5000

# کشتن پروسس
kill -9 <PID>
```

### مشکل در ارتباط بین سرویس‌ها:

```bash
# بررسی شبکه
docker network inspect zambeel-network

# اتصال مجدد به شبکه
docker network connect zambeel-network <container_name>
```

### لاگ خطاها:

```bash
# مشاهده خطاهای فرانت‌اند
docker logs zambeel-frontend-container

# مشاهده خطاهای بک‌اند
docker logs zambeel-backend-container

# مشاهده خطاهای اسکریپر
docker logs zambeel-scraper-container
```

## 📝 نکات مهم

1. **اولین بار**: حتما `./docker.sh build` را قبل از `./docker.sh up` اجرا کنید
2. **تغییرات کد**: بعد از هر تغییر در کد، باید ایمیج‌ها را دوباره بسازید
3. **پورت‌ها**: مطمئن شوید پورت‌های 3000، 8080 و 5000 آزاد هستند
4. **شبکه**: همه سرویس‌ها باید در شبکه `zambeel-network` باشند

## 🆘 دستورات اضطراری

```bash
# حذف همه کانتینرها و ایمیج‌ها
docker-compose down
docker rmi zambeel-frontend zambeel-backend zambeel-scraper

# پاکسازی کامل Docker
docker system prune -a

# حذف شبکه
docker network rm zambeel-network
```

## ✅ چک‌لیست راه‌اندازی

- [ ] Docker نصب شده است
- [ ] Docker Compose نصب شده است
- [ ] پورت‌های 3000، 8080، 5000 آزاد هستند
- [ ] اسکریپت `docker.sh` قابل اجرا است (`chmod +x docker.sh`)
- [ ] دستور `./docker.sh up` اجرا شده است
- [ ] همه سرویس‌ها با دستور `./docker.sh status` فعال هستند
- [ ] می‌توانید به http://localhost:3000 دسترسی داشته باشید

---

برای اطلاعات بیشتر، فایل `DOCKER.md` را مطالعه کنید.
